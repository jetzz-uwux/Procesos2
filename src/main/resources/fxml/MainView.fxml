<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.control.Button?>
<?import javafx.scene.control.TableColumn?>
<?import javafx.scene.control.TableView?>
<?import javafx.scene.control.TextField?>
<?import javafx.scene.layout.BorderPane?>
<?import javafx.scene.layout.HBox?>
<?import javafx.scene.layout.Region?>

<BorderPane xmlns="http://javafx.com/javafx/25" xmlns:fx="http://javafx.com/fxml/1" fx:controller="progs.proyectobibliotecafx.controller.MainViewController">

    <!-- TOP -->
    <top>
        <HBox alignment="CENTER_LEFT" spacing="15" style="-fx-padding: 10;">
            <Button fx:id="btn_reportes" onAction="#handleReportes" text="Reportes" />
            <Button fx:id="btn_prestamo" onAction="#handlePrestamos" text="Préstamos" />
            <Button fx:id="btn_socios" onAction="#handleSocios" text="Socios" />

            <Region HBox.hgrow="ALWAYS" />

            <TextField fx:id="txtBuscar" promptText="Buscar libro..." />
            <Button fx:id="btn_buscar" onAction="#handleBuscar" text="Buscar" />
        </HBox>
    </top>

    <!-- CENTER -->
    <center>
        <TableView fx:id="tablaLibros">
            <columns>
                <TableColumn fx:id="colIsbn" text="ISBN" />
                <TableColumn fx:id="colTitulo" text="Título" />
                <TableColumn fx:id="colEdicion" text="Edición" />
                <TableColumn fx:id="colAnio" text="Año" />
                <TableColumn fx:id="colFormato" text="Formato" />
                <TableColumn fx:id="colPaginas" text="Páginas" />
            </columns>
        </TableView>
    </center>

    <!-- BOTTOM -->
    <bottom>
        <HBox alignment="CENTER" spacing="30" style="-fx-padding: 10;">
            <Button fx:id="btn_prestar" onAction="#handlePrestar" styleClass="boton-redondo" text="Prestar" />
            <Button fx:id="btn_nuevo" onAction="#handleNuevo" styleClass="boton-redondo" text="Nuevo" />
            <Button fx:id="btn_editar" onAction="#handleEditar" styleClass="boton-redondo" text="Editar" />
            <Button fx:id="btn_eliminar" onAction="#handleEliminar" styleClass="boton-redondo" text="Eliminar" />
        </HBox>
    </bottom>

</BorderPane>
